/*
  There are several options for sending message between package and chrome extension. These include web socket's and chrome's native messaging
*/

// core logic for rewind feature
const formatAndSendToChrome = () => {
  // placeholder
  console.log('formatAndSendToChrome inovked');
  return;
};

export default formatAndSendToChrome;